create database Ordenador;
use Ordenador;
create table Data(
	id int auto_increment primary key,
    size int,
	time varchar(100)
);

create table Data2(
	id int auto_increment primary key,
    name varchar(100),
	size int,
	time varchar(100)
);
